{
  "version": 2,
  "buildCommand": "cd .. && npm run build --workspace=shared && npm run build --workspace=backend",
  "installCommand": "cd .. && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
